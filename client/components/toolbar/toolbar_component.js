import React from 'react'
import {ToolbarContent} from '../../components'
const Toolbar = () => {
  return (
    <section className="toolbar">
      <ToolbarContent />
    </section>
  )
}

export default Toolbar
